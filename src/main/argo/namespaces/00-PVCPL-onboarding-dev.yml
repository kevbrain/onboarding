kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: onboarding-dev-pipeline-claim
  namespace: onboarding-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-ceph-rbd
  volumeMode: Filesystem
status:

